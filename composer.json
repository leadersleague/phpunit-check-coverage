{
    "name": "leadersleague/phpunit-check-coverage",
    "description": "This program is to be integrated into your continuous integration processes in order to be able to stop it (fail) if the test coverage rate is lower than that set by the team. Thus we avoid regressions following the addition of new lines of code.",
    "autoload": {
        "psr-4": {
            "Coverage\\": "src/",
            "Test\\": "tests/"
        }
    },
    "require": {
      "ext-simplexml": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^7.5"
    }
}
